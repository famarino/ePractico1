<html>
    <head></head>
    <body>
        <table class="default" align="center">
            <tr>
                <th colspan="2">Ejercicio Pokemons</th>
            </tr>
            <tr > 
                <th>Pokemon</th>
                <th>Url</th>
            </tr>
            <% pkmons.forEach(function(item) { %>
            <tr>
                <td><%=item.name%></td>
                <td><a href="<%=item.url%>"><%=item.url%></td>
                <% }); %>
            </tr>
            
        </table>
    </body>
</html>